export class DataScale {
    startValue: number;
    pixelOffset: number;
    deltaValue: number;
    deltaPixel: number;
}

export class TimeScale {
    startDate: Date;
    pixelOffset: number;
    deltaSecond: number;
    deltaPixel: number;
}